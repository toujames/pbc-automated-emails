name: Celebration Emails

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Log matching emails without sending"
        required: true
        default: "true"
        type: choice
        options:
          - "true"
          - "false"
      preview_event:
        description: "Send a sample template to the test email"
        required: true
        default: "none"
        type: choice
        options:
          - "none"
          - "birthday"
          - "anniversary"
  schedule:
    - cron: "0 14 * * *"

jobs:
  send:
    runs-on: ubuntu-latest
    env:
      PC_CLIENTID: ${{ secrets.PC_CLIENTID }}
      PC_SECRET: ${{ secrets.PC_SECRET }}
      RESEND_API: ${{ secrets.RESEND_API }}
      EMAIL_FROM: "PBC <info@pbctulsa.org>"
      EMAIL_REPLY_TO: ${{ vars.EMAIL_REPLY_TO }}
      TEST_EMAIL: ${{ github.event_name == 'workflow_dispatch' && 'james@touthang.info' || '' }}
      PREVIEW_EVENT: ${{ github.event_name == 'workflow_dispatch' && inputs.preview_event || 'none' }}
      TIME_ZONE: America/Chicago
      DRY_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || 'false' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Send celebration emails
        run: npm run send
